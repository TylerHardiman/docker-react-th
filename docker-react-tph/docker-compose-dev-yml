version: '3.9'

services:

    docker-react-dev:
        container_name: 394204d45d87
        build:
            context: .
            image: 
            dockerfile: Dockerfile-dev
        volumes:
            - .:/app
            - '/app/node_modules'
        ports:
            - '8001:8000'

    docker-react-test:
        container_name: docker_react_test
        image: docker-react-tph_docker-react-dev #test for testing
        volumes:
            - .:/app
            - '/app/node_modules'
        command: ["npm","run","test",]